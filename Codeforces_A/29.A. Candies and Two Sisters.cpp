#include<bits/stdc++.h>
using namespace std;

int32_t main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int tst; cin >> tst;
    while (tst--) {
        int cndy; cin >> cndy;
        int ways = (cndy - 1) / 2;
        cout << ways << '\n';

    }
    return 0;
}
